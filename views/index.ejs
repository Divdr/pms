<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
  </head>
  <body>
    <div class="container">
      <div class="row">
        <h2 class="text-center col-md-12 text-primary"><%= title %></h2>
        <div class="panel panel-info col-md-10 offset-md-2">
          <div class="panel-heading" style="margin-bottom: 20px;">
            <div class="panel-title h2 text-info">Login</div>
          </div>
          <div class="panel-body">
            
            <form action="/" method="post" id="loginForm" class="form-horizontal" role="form" autocomplete="false">
              <% if(msg != ''){ %>
                <div class="alert alert-danger">
                  <h4 class="text-center"><%= msg %></h4>
                </div>
              <% } %>
              <div class="form-group">
                <label for="username">Username</label>
                <div class="col-md-9">
                  <input type="text" name="uname" class="form-control" placeholder="Enter Username">
                  <%if(errors != '' && errors.uname){ %>
                    <span class="text-danger">
                      <%= errors.uname.msg %>
                    </span>
                  <% } %>
                </div>
              </div>
              <div class="form-group">
                <label for="password">Password</label>
                <div class="col-md-9">
                  <input type="password" name="password" class="form-control" placeholder="Enter Password">
                  <%if(errors != '' && errors.password){ %>
                    <span class="text-danger">
                      <%= errors.password.msg %>
                    </span>
                  <% } %>
                </div>
              </div>
              <div class="form-group">
                <button type="submit" class="btn btn-info" id="loginbtn">login</button>
              </div>
              <p>Create New Account
                <a href="/signup" style="text-decoration: none;">Click Here</a>
              </p>
            </form>
          </div> 
        </div>
      </div>
    </div>

  </body>
</html>
